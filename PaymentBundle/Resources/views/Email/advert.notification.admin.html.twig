{% block subject %}{{ 'advert.notification.email.subject' | trans({ '%order_number%': order_number }) }}{% endblock %}

{% block body_text %}
{% autoescape false %}
    
{{ 'advert.notification.email.hello' | trans }}

{{ 'advert.notification.email.date' | trans({ '%date%': invoice_date | date('d/m/Y') }) }}

{{ 'advert.notification.email.buyer' | trans({ '%buyer_email%': user_email }) }}

{{ 'advert.notification.email.url' | trans({ '%url%': order_details_url }) }}
{% endautoescape %}
{% endblock %}

{% block body_html %}
    {{ include('CoreBundle:Email:_header.html.twig') }}
    
    <p style="padding:20px 0; margin:0;">{{ 'advert.notification.email.hello' | trans }}</br>

    <p style="padding:5px 0; margin:0;">{{ 'advert.notification.email.date' | trans({ '%date%': invoice_date | date('d/m/Y') }) }}</br>

    <p style="padding:5px 0; margin:0;">{{ 'advert.notification.email.buyer' | trans({ '%buyer_email%': user_email}) }}</br>
    
    <p style="padding:5px 0; margin:0;">{{ 'advert.notification.email.url' | trans({ '%url%': order_details_url }) }}</p>
    
    {{ include('CoreBundle:Email:_footer.html.twig') }}
{% endblock %}